# Claude Code 지침

**작업을 시작하기 전에 [`AGENTS.md`](AGENTS.md)를 읽는다.** 이 저장소의 에이전트 운영 규칙은
거기 한 곳에 모여 있다. Claude Code 말고도 여러 도구가 이 저장소를 만지므로, 규칙을
도구마다 복사해 두지 않고 한 파일로 관리한다. 이 파일에는 Claude Code 에만 해당하는 것만 적는다.

## 새 작업은 worktree 에서 시작한다 (필수)

`EnterWorktree` 도구로 격리된 worktree 를 만들고 그 안에서 작업한다.
`git checkout -b` 로 현재 디렉토리에서 브랜치만 바꾸지 않는다.

이 지시가 여기 있는 이유가 있다. `EnterWorktree` 는 사용자가 직접 요청하거나
CLAUDE.md 가 지시할 때만 쓰도록 되어 있어서, 이 문장이 없으면 도구가 발동하지 않는다.

worktree 를 만든 뒤 Unity Library 를 복사해 두면 첫 임포트가 짧아진다.
사유와 세부 절차는 AGENTS.md 1절에 있다.

## 사용자에게 물어야 하는 것

- **UI 결정**: 화면·조작을 새로 만들거나 바꿀 때는 해결하려는 문제와 만들려는 UI를
  먼저 알리고 확인을 받는다. 곧장 만들지 않는다.
- **난이도 수치**: 마찰·속도·경로·로프 규격처럼 재미를 좌우하는 값은 임의로 정하지 않는다.
  근거(테스트 실측)를 대거나, 근거가 없으면 임의값임을 밝힌다.
- **남의 미커밋 변경 발견**: 작업 트리에 다른 작업이 있으면 멈추고 알린다(AGENTS.md 2절).

## 버그를 고칠 때

원인이 애매하거나 해법이 갈리면 `/yfix` 절차를 따른다. 코드 근거(`파일:라인`)로 원인을
먼저 규명하고, 해법 후보를 트레이드오프로 비교한 뒤 사용자에게 방향을 묻는다.

물리 관련 수정은 특히 그렇다. 이 프로젝트에서 "정지 상태에서 좋아 보이던 설정이
주행 중에 짐을 날려 버린" 일이 실제로 있었다. 원인을 수치로 짚지 않으면 반대 방향으로
고치게 된다.
